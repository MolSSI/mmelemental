name: test
channels:
dependencies:
    # Base depends
  - python>=3.9
  - pip
  - docutils=0.16

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - pint
    - pyyaml
    - git+https://github.com/MolSSI/mm_data
    - cmselemental
    - sphinx
    - sphinx-rtd-theme
    - sphinx_autodoc_typehints
    - sphinx_automodapi
