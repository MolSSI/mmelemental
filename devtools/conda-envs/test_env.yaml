name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - qcelemental
    - codecov
    - sphinx
    - asteroid-sphinx-theme
    - git+https://github.com/MolSSI/mm_data
    - git+https://github.com/MolSSI/cmselemental@dev
